# package.use/00ffmpeg
#

# create a randomized package list from USE flags:
#
# for u in ... ; do quse -NC $u 2>/dev/null; done | cut -f1-2 -d'/' | sort -u | shuf
#

# these USE flags activates ffmpeg
#
*/*             acoustid avformat convert cuda exif ffmpeg gd swscale system-ffmpeg vcodec2 video void vpx x264 x265 xine

# these USE flags are often needed too to resolve deps
#
*/*             X icu messages mikmod mms mod netpbm python qml svc truetype xkb
