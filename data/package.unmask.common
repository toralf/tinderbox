# package.unmask/common
#
# this file resides in /tmp/tb/data within an tinderbox image
# and is symlinked into its right location under /etc/portage

# please note that this file rules over /etc/portage/package.mask/self
# so best is to let it be empty
# file individual entries within each tinderbox image instead
