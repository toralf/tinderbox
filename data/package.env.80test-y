# package.env/80test-y
#

# packages excluded for FEATURES=test tinderboxing


#----------------------------------------------------------------------
# whole categories
#----------------------------------------------------------------------

# too often circ deps in @world
dev-ruby/*                          notest

# too much pytest issues et al
dev-python/*                        notest

# require special kernel config
net-wireless/*                      notest

# requested by Soap
sci-*/*                             notest


#----------------------------------------------------------------------
# single packages
#----------------------------------------------------------------------

# OOM killer
app-text/podofo                     notest

# kernel specific config
dev-libs/ell                        notest

# tmpfs has no xattr
dev-python/pyxattr                  notest

# requires /dev/fuse
dev-util/tup                        notest

# test fails if system dbus instance is not running: https://bugs.gentoo.org/632082
gnome-base/dconf                    notest

# https://bugs.gentoo.org/743938#c4 bubblewrap does not provide /sys
media-tv/kodi                       notest

# forces +X
dev-util/cmake                      notest

# You must have CONFIG_KEY_DH_OPERATIONS to run the package testsuite!
sys-apps/keyutils                   notest

# needed immediately after setup
app-portage/gentoolkit              notest
sys-apps/portage                    notest

# setup issues
dev-libs/glib                       notest
dev-libs/gobject-introspection      notest
dev-libs/libical                    notest
dev-lua/lutok                       notest
dev-qt/qtchooser                    notest
media-libs/libepoxy                 notest
media-libs/libglvnd                 notest
sys-apps/dbus                       notest

# https://bugs.gentoo.org/802579
net-libs/nodejs                     notest

# hangs due to glibc-2.34
sys-apps/gawk                       notest

# Need FEATURES=-userpriv to run this testsuite
sys-libs/libhugetlbfs               notest


#----------------------------------------------------------------------
# too long emerge time
#
# for i in ~/img/*test*/; do echo; echo $i; qlop --merge --time -f $i/var/log/emerge.log | grep -E -e ': [1-9 ]*.:..:..' ; done | grep '>>>' | cut -f4,3 -d' ' | sort -u -k 2
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# killed manually due to too long runtime
#----------------------------------------------------------------------
