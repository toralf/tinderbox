# package.use/common
#

# deliver SMTP emails w/ authentication and secure to us
#
mail-mta/ssmtp    ssl mta

# otherwise it gives: HTTPS support not compiled in.
#
net-misc/wget     ssl

# known blocker
#
app-editors/emacs -dbus -svg -ssl -gtk -gtk3
dev-util/cmake    -qt4 -qt5
media-libs/mesa   -vaapi